{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Mangrove",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}/build",
            "output": "build/Mangrove.exe"
        }
    ]
}